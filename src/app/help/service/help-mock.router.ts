/**
 * Mock Class for Angular-Router in HelpService
 */

export class HelpMockRouter {
  events: any = {
    subscribe: (fuction: any) => {
      // To make the linter happy (tslint: block is empty)
    }
  };
}
